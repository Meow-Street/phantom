/// The renderer for the application. This is a placeholder for now.
pub struct Renderer {
    // device: wgpu::Device,
    // queue: wgpu::Queue,
}

impl Renderer {
    pub fn new() -> Self {
        Self {}
    }
}
